server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Khanh@2005
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
     maximum-pool-size: 10
     minimum-idle: 5
     idle-timeout: 30000
     connection-timeout: 20000

  jackson:
    property-naming-strategy: SNAKE_CASE

  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    com.khanh.ordermanagement: DEBUG
    org.springframework.jdbc.core.JdbcTemplate: DEBUG